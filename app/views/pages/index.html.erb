  <div class="row">
    
    <div class="panel" id="search_panel">
      <h3> Pick your neighborhood: </h3>
      <%= search_form_for @search do |f| %>
        <%=
          f.select :neighborhood_name_eq,
          options_for_select(
                @hoods
              )
        %>
        <%=f.submit %>
      <%end %>
    </div>

  </div>

